DATABASE_URL=postgres://postgres:postgres@localhost:5432/muscle_recovery
PORT=3000
NODE_ENV=development
# Optional only when the frontend is served from another origin, e.g. https://app.example.com
FRONTEND_ORIGIN=
